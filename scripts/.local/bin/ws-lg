#!/usr/bin/env bash
set -euo pipefail

if ! command -v lazygit >/dev/null 2>&1; then
  printf 'Error: lazygit is required but was not found in PATH.\n' >&2
  exit 1
fi

if [[ -n "${ZELLIJ:-}" ]]; then
  exec zellij run -f -c --name "lazygit" --cwd "$PWD" -- lazygit
fi

exec lazygit
