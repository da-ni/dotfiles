{
  "custom/weather": {
    "format": "{}°",
    "tooltip": true,
    "interval": 300,
    "exec": "wttrbar --nerd --location \"Vienna,Austria\"",
    "return-type": "json",
    "on-click": "curl -s 'wttr.in/Vienna?format=3' | xargs -I {} notify-send 'Weather' {}"
  }
}
